apiVersion: v1
kind: Service
metadata:
  name: {{ include "efk-monitoring.fullname" . }}
  labels:
    {{- include "efk-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "efk-monitoring.selectorLabels" . | nindent 4 }}
